#include <stdio.h>
int main()
{
    int x, y, average;
    int op;
    printf("Choose Option : ");
    scanf("%d", &op);
    switch (op)
    {
    case 1:
    {
        printf("Enter X : ");
        scanf("%d", &x);
        printf("Enter Y : ");
        scanf("%d", &y);
    }
    };